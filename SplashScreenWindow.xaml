<!-- SplashScreenWindow.xaml -->

<Window
    x:Class="HyperZoneFRPGUILauncherforWindows.SplashScreenWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Border x:Name="RootBorder" 
            Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
            CornerRadius="8">

        <Grid>

            <StackPanel HorizontalAlignment="Center" 
                        VerticalAlignment="Center" 
                        Spacing="16">

                <Image Source="/Assets/Logo.png" 
                       Width="128" 
                       Height="128" />

                <StackPanel Orientation="Horizontal" 
                            Spacing="12" 
                            HorizontalAlignment="Center"
                            Margin="0,24,0,0">

                    <ProgressRing IsActive="True" 
                                  Width="20" 
                                  Height="20"/>

                    <TextBlock Text="HyperZoneFRP GUI Launcher for Windows正在启动..." 
                               FontSize="14" 
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>

            </StackPanel>
            <StackPanel VerticalAlignment="Bottom" 
                        HorizontalAlignment="Center"
                        Margin="0,0,0,24"
                        Spacing="4">

                <TextBlock Text="连接你我，穿透未来"
                           FontSize="16"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center" />

                <TextBlock Text="© 2025 HyperZoneFRP. All Rights Reserved."
                           FontSize="12"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
